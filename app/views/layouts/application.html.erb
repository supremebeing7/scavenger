<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title)+" | Scavenger" : "Scavenger" %></title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag 'favicon.ico' %>
</head>
<body>
<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <%= link_to "Scavenger", root_url,
        :class => "navbar-text navbar-left" %>
    <% if user_signed_in? %>
      <%= link_to "Logout", destroy_user_session_path, method: :delete,
          :class => "navbar-text navbar-right" %>
      <%= image_tag current_user.photo_url, :class => "nav-photo navbar-right" %>
      <%= link_to current_user.name, edit_user_registration_path,
          :class => "navbar-text navbar-right" %>
      <span class="navbar-text navbar-right">Logged in as </span>
    <% else %>
      <%= link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2), :class => "navbar-text navbar-right" %>
      <%= link_to "Sign in", new_user_session_path,
          :class => "navbar-text navbar-right" %>
      <%= link_to "Sign up", new_user_registration_path,
          :class => "navbar-text navbar-right" %>
    <% end %>
  </div>
</div>

<div class="container" id="main">
  <div class="bold-red">
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, id: "flash_#{name}" %>
    <% end %>
  </div>
  <%= yield %>
</div>

</body>
</html>
